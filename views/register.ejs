<!DOCTYPE html>
<html>
  <head>
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Register</title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <style>
      body {
        font-family: "Poppins", sans-serif;
      }
    </style>
  </head>
  <body
    class="bg-gradient-to-br from-blue-100 to-purple-100 min-h-screen flex flex-col"
  >
    <nav class="bg-white shadow-lg p-4 flex justify-between items-center z-10">
      <h1 class="text-3xl font-bold text-blue-700 tracking-wide">SIMARIN</h1>
      <a
        href="/"
        class="text-gray-700 hover:text-blue-600 transition duration-300 ease-in-out text-lg"
        >Beranda</a
      >
      <div class="flex items-center space-x-6">
        <a
          href="/login"
          class="bg-blue-600 text-white px-6 py-2 rounded-full shadow-md hover:bg-blue-700 transition duration-300 ease-in-out text-lg"
          >Login</a
        >
        <a
          href="/register"
          class="text-gray-700 hover:text-blue-600 transition duration-300 ease-in-out text-lg"
          >Register</a
        >
      </div>
    </nav>

    <div class="flex-grow flex items-center justify-center p-6">
      <div
        class="bg-white p-10 rounded-xl shadow-2xl space-y-7 max-w-md w-full transform transition-all duration-500 hover:scale-105"
      >
        <h1 class="text-3xl font-bold text-center text-gray-800">
          Yuk, Buat Akun Baru! ✨
        </h1>
        <p class="text-center text-gray-600 text-lg">
          Silakan daftar untuk membuat akun Anda.
        </p>
        <% if (typeof error !== 'undefined' && error) { %>
        <div
          class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4"
        >
          <strong><%= error %></strong>
        </div>
        <% } %>

        <form
          action="/auth/register"
          method="POST"
          class="space-y-6"
          onsubmit="return validateForm()"
        >
          <!-- Username -->
          <div>
            <label
              for="username"
              class="block text-gray-700 font-medium mb-2 text-lg"
            >
              Username <span class="text-red-500">*</span>
            </label>
            <input
              type="text"
              name="username"
              id="username"
              placeholder="Pilih username Anda"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
              required
            />
          </div>

          <!-- Password -->
          <div>
            <label
              for="password"
              class="block text-gray-700 font-medium mb-2 text-lg"
            >
              Password <span class="text-red-500">*</span>
            </label>
            <input
              type="password"
              name="password"
              id="password"
              placeholder="Buat password Anda (min. 4 karakter)"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
              required
              minlength="4"
            />
          </div>

          <!-- Konfirmasi Password -->
          <div>
            <label
              for="confirmPassword"
              class="block text-gray-700 font-medium mb-2 text-lg"
            >
              Konfirmasi Password <span class="text-red-500">*</span>
            </label>
            <input
              type="password"
              name="confirmPassword"
              id="confirmPassword"
              placeholder="Masukkan kembali password Anda"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
              required
            />
          </div>

          <!-- No HP -->
          <div>
            <label
              for="phone"
              class="block text-gray-700 font-medium mb-2 text-lg"
              >No HP</label
            >
            <div class="flex">
              <select
                name="countryCode"
                class="p-3 border border-gray-300 rounded-l-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent text-lg"
              >
                <option value="+62" selected>Indonesia (+62)</option>
                <!-- Tambahkan kode lain kalau mau -->
              </select>
              <input
                type="text"
                name="phone"
                id="phone"
                placeholder="81234567890"
                class="w-full p-3 border border-gray-300 rounded-r-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
              />
            </div>
          </div>

          <!-- Email -->
          <div>
            <label
              for="email"
              class="block text-gray-700 font-medium mb-2 text-lg"
              >Email</label
            >
            <input
              type="email"
              name="email"
              id="email"
              placeholder="Alamat email"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
            />
          </div>

          <!-- Tanggal Lahir -->
          <div>
            <label
              for="dob"
              class="block text-gray-700 font-medium mb-2 text-lg"
              >Tanggal Lahir</label
            >
            <input
              type="date"
              name="dob"
              id="dob"
              class="w-full p-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent transition duration-200 text-lg"
            />
          </div>

          <!-- Tombol Daftar -->
          <button
            type="submit"
            class="bg-blue-600 text-white w-full py-3 rounded-lg shadow-md hover:bg-blue-700 transition duration-300 ease-in-out font-semibold text-xl transform hover:scale-100"
          >
            Daftar Akun
          </button>
        </form>
        <p class="text-center text-gray-600 text-md mt-6">
          Sudah punya akun?
          <a
            href="/login"
            class="text-blue-600 hover:text-blue-800 font-semibold transition duration-300 ease-in-out"
            >Login di sini</a
          >
        </p>
      </div>
    </div>

    <!-- JS Validasi Konfirmasi Password -->
    <script>
      function validateForm() {
        const pw = document.getElementById("password").value;
        const cpw = document.getElementById("confirmPassword").value;

        if (pw !== cpw) {
          alert("Konfirmasi password tidak cocok!");
          return false;
        }
        return true;
      }
    </script>
  </body>
</html>
