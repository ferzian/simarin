<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Alur Selesai Magang</title>
  <!-- Tailwind -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- SweetAlert2 -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body class="bg-gradient-to-br from-gray-100 to-gray-200 min-h-screen font-sans">

  <div class="max-w-6xl mx-auto py-12 px-6">
    <h1 class="text-4xl font-extrabold mb-12 text-center text-gray-800 tracking-wide">
      🚀 Alur Selesai Kegiatan Magang
    </h1>

    <div class="grid md:grid-cols-3 gap-10">
      <!-- Step 1 -->
      <div 
        onclick="openStep('Survey Kepuasan', 'https://ptsp.kkp.go.id/skm/s/3327')"
        class="bg-gradient-to-br from-green-400 to-green-500 p-8 rounded-2xl shadow-xl hover:scale-105 transform transition duration-300 cursor-pointer text-white text-center relative">
        <div class="flex justify-center mb-4">
          ✅
        </div>
        <h2 class="text-2xl font-bold mb-2">1. Isi Survey Kepuasan</h2>
        <p class="opacity-90">Peserta wajib mengisi survei kepuasan pada link resmi KKP.</p>
      </div>

      <!-- Step 2 -->
      <div 
        onclick="openStep('Upload Laporan', '/user/laporan')"
        class="bg-gradient-to-br from-yellow-400 to-yellow-500 p-8 rounded-2xl shadow-xl hover:scale-105 transform transition duration-300 cursor-pointer text-white text-center relative">
        <div class="flex justify-center mb-4">
          📑
        </div>
        <h2 class="text-2xl font-bold mb-2">2. Upload Laporan</h2>
        <p class="opacity-90">Kumpulkan laporan magang maksimal 1 bulan setelah kegiatan.</p>
      </div>

      <!-- Step 3 pakai if else -->
      <% if (typeof statusLaporan !== "undefined" && statusLaporan === "approved") { %>
        <!-- Aktif kalau laporan disetujui -->
        <div 
          onclick="openStep('Unduh Sertifikat', '/user/sertifikat')"
          class="bg-gradient-to-br from-purple-400 to-purple-600 p-8 rounded-2xl shadow-xl hover:scale-105 transform transition duration-300 cursor-pointer text-white text-center relative">
          <div class="flex justify-center mb-4">
            🎓
          </div>
          <h2 class="text-2xl font-bold mb-2">3. Unduh Sertifikat</h2>
          <p class="opacity-90">Sertifikat dapat diunduh setelah laporan disetujui admin.</p>
        </div>
      <% } else { %>
        <!-- Terkunci kalau belum approve -->
        <div 
          class="bg-gradient-to-br from-gray-300 to-gray-400 p-8 rounded-2xl shadow-xl text-white text-center relative opacity-60 cursor-not-allowed">
          <div class="flex justify-center mb-4">
            🔒
          </div>
          <h2 class="text-2xl font-bold mb-2">3. Unduh Sertifikat</h2>
          <p class="opacity-90">Menunggu persetujuan laporan dari admin.</p>
        </div>
      <% } %>
    </div>
  </div>

  <script>
    function openStep(step, url) {
      Swal.fire({
        title: step,
        text: `Apakah Anda ingin melanjutkan ke bagian ${step}?`,
        icon: 'question',
        showCancelButton: true,
        confirmButtonColor: '#2563eb',
        cancelButtonColor: '#d33',
        confirmButtonText: 'Ya, lanjut!'
      }).then((result) => {
        if (result.isConfirmed) {
          window.open(url, "_blank"); 
        }
      });
    }
  </script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('laporan-container');
    const form = document.getElementById('upload-form');

    // Hanya pasang listener sekali
    if (!form.dataset.listener) {
      form.dataset.listener = true; // tandai listener sudah dipasang

      form.addEventListener('submit', async (e) => {
        e.preventDefault(); // cegah reload halaman
        container.innerHTML = ''; // bersihkan DOM sebelumnya

        const file = form.laporan.files[0];
        if(file){
          const div = document.createElement('div');
          div.textContent = file.name + ' (akan diupload...)';
          div.className = 'p-4 bg-gray-200 rounded shadow';
          container.appendChild(div);
        }

        // Kalau mau lanjut ke server, panggil fetch/ajax di sini
      });
    }
  });
</script>

</body>
</html>
